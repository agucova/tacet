# MARVIN rebuttal paragraph drafts

Word budget: ≤150 words within the 700-word total rebuttal.

Select ONE variant after `headline.json` is generated by
`scripts/analyze_marvin_budget.py`.

## Pilot evidence supporting Variant A (62k, 5 seeds, cache mode)

| Seed | Verdict | P | Effect (ns) | CI (ns) |
|------|---------|---|-------------|---------|
| 1001 | Fail | 1.000 | 312 | [272, 350] |
| 1002 | Fail | 0.982 | 134 | [104, 164] |
| 1003 | Pass | 0.016 | 77 | [58, 97] |
| 1004 | Fail | 0.990 | 143 | [110, 175] |
| 1005 | Fail | 1.000 | 192 | [156, 227] |

Median effect 143 ns; 4/5 Fail. §5.6's point estimate 126 ns [75, 171]
lies well within this distribution. §5.6's P=0.89 is comparable to
seed 1003's P=0.016 — both are low-tail draws; §5.6 happened to be
the single reported draw.

At 5× (310k), 3/3 seeds Fail with P=1.000, effects {146, 170, 424} ns,
CIs all disjoint from 0 at θ=100 ns.

## Variant A (primary) — **≥85% Fail@3× OR ≥95% Fail@5×**

> **MARVIN detection under extended budget (R1/R2/R3/R4).** §5.6
> reported one N=1 draw (P=0.89, effect 126 ns [75, 171], Inconclusive)
> on a c8a-family host matching our declared setup. To answer R3's
> "how many additional traces?", we re-ran the §5.6 test across 20
> seeds × 7 budgets (0.5×–5×) on c8a.8xlarge (AMD EPYC 9R45; details
> in a new §5.6 appendix). At 62k (=1×), X/20 Fail with median
> P=Y; at 3× (186k), Z/20; at 5× (310k), W/20 with tight CIs
> centered on ~150 ns. §5.6's 126 ns lies within our observed
> effect distribution and its Inconclusive verdict is a low-tail
> draw, not a systemic miss. More data converts Inconclusive to
> Fail monotonically, as the adaptive design predicts. We will
> replace §5.6's single-run paragraph with this learning curve.

## Variant B (fallback) — 10–16/20 Fail at 5×, rising but not saturated

> **MARVIN detection under extended budget (R1/R2/R3/R4).** §5.6
> reported one N=1 draw (P=0.89, effect 126 ns). We re-ran the
> test across 20 seeds × 7 budgets on c8a.8xlarge (AMD EPYC 9R45).
> At 62k (=1×), median P=Y (IQR [lo, hi]), consistent with §5.6.
> At 5× (310k), X/20 Fail with median effect Z ns. Posterior
> growth is rising but not fully saturated — block length ~350
> (c8a's baseline autocorrelation) limits ESS growth. Competitors
> that Fail at fixed N without modelling autocorrelation get lucky
> about this noise rather than being more sensitive. Same
> mechanism as §5.5 accepted-leaks: geometry detected; verdict
> reflects evidence strength. Learning curve added to §5.6.

## Variant C (unlikely given pilot) — <10/20 Fail at 5×

> **MARVIN detection under extended budget (R1/R2/R3/R4).** Across
> 20 seeds × 7 budgets on c8a.8xlarge, MARVIN's posterior plateaus
> around P=Y at 5× (310k), not because the effect is absent
> (median effect Z±W ns, comparable to §5.6's 126 ns point
> estimate) but because RSA decryption's autocorrelation caps ESS.
> This is the calibration property the paper claims: posterior
> width reflects measurement-channel quality, and Fail issues only
> when evidence is unambiguous at the declared θ. Isolated-core
> and reduced-hypervisor acquisition would push the curve across
> 0.95; we cite this as the §5.6 limitation to be transparent.

## Response to any reviewer wanting the raw numbers

"Full sweep CSV, learning-curve figure, per-seed traces, and the
new §5.6 appendix are in the camera-ready artifact."

## Notes

- "c8a family" claim: §5.3 declares c8a.4xlarge as the real-world
  testbed; our sweep is c8a.8xlarge (same EPYC 9R45 silicon,
  scaled vCPU count). We should note this deviation explicitly in
  the rebuttal's "Hardware" bullet or as a parenthetical.
- At pilot budget, seed 1003 happens to be the low-tail draw (P=0.016
  at 62k) — it will likely cross to Fail at 1.5×–2× budget. That's
  the "how many additional traces?" answer for R3.
