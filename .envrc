# Allow insecure python3-ecdsa-0.19.1 (CVE-2024-23342), required transitively by
# tlsfuzzer. devenv.yaml's permittedInsecurePackages is bypassed by a predicate
# mismatch in devenv 2.0.6, so set the env var escape hatch here.
export NIXPKGS_ALLOW_INSECURE=1

use devenv
